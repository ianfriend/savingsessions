[tool.poetry]
name = "savingsessions"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
streamlit = "^1.41.1"
pendulum = "^3.0.0"
rich = "^13.7.0"
supabase = "^2.2.1"
dateparser = "^1.2.0"

[tool.poetry.group.dev.dependencies]
ruff = "^0.1.6"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
check_free_electricity = "savingsessions.scripts.check_free_electricity:main"
